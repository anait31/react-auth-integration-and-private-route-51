// import { useContext } from "react";
// import { AuthContext } from "../../providers/AuthProviders"; Note: Nust need to use bracket if the exporter is not default
// import AuthContext from "../../providers/AuthProviders";

const Home = () => {

    // const authInfo = useContext(AuthContext);
    // console.log(authInfo);

    return (
        <div>
            <h2>This is Home </h2>
        </div>
    );
};

export default Home;